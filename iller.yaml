---
doctype: dist

header:
  name: String-Stomp

prereqs:
  runtime:
    requires:
      - perl: 5.010000

plugins:
 - +config: Author::CSSON

 - +add_plugin: TravisYML
   +before: Git::Tag
   perl_version: 5.10 5.14 5.20

---
doctype: weaver

plugins:
  - +config: Author::CSSON

  - +remove_plugin: Splint
